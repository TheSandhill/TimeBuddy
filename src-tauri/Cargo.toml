[package]
name = "timebuddy"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "timebuddy_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# `tray-icon`: close minimises to the tray and quitting lives in its menu
# (ADR-0004), so the icon is not optional decoration — it is the way out.
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-sql = { version = "2", features = ["sqlite"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = ["sqlite", "runtime-tokio", "chrono", "macros"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
rust_xlsxwriter = { version = "0.97.1", default-features = false, features = ["chrono"] }
tauri-plugin-dialog = "2.7.2"
tauri-plugin-autostart = "2.5.1"
tauri-plugin-notification = "2.3.3"
# Shipping is a `git tag` and updating is one click (ADR-0001, ADR-0009). The
# installer is unsigned; the *update* is signed with a minisign key, which is
# what stops the one public URL in the binary from being an install-anything
# hole.
tauri-plugin-updater = "2.9.0"
# Only for the relaunch after an update is installed. It exits the process
# rather than closing the window — which is the point, because closing the
# window hides it (ADR-0004).
tauri-plugin-process = "2.3.1"
# The lock screen's one primitive (ADR-0003). `std` brings `OsRng`, which is
# what salts a hash and what makes a "remember me" token unguessable.
argon2 = { version = "0.5", features = ["std"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
# An .xlsx is a zip of XML. The export tests open one and read the cells, so
# "the numbers are numbers" is checked rather than hoped for.
zip = { version = "8", default-features = false, features = ["deflate"] }
# The backup tests rotate real files in a real folder. Rotation deletes things,
# so it is checked where a mistake costs a temporary directory and nothing else.
tempfile = "3"

